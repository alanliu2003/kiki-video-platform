# Local development profile.
# PostgreSQL is available via docker compose but is not required
# for the API to start in Milestone 1.

server:
  port: ${SERVER_PORT:8080}

# Future persistence settings (unused until a later milestone):
# spring:
#   datasource:
#     url: ${POSTGRES_URL:jdbc:postgresql://localhost:5432/video_platform}
#     username: ${POSTGRES_USER:video}
#     password: ${POSTGRES_PASSWORD:video}
